                    player 使用的Properties 
                    -----------------------

User 使用標準 living 所包含的所有 Props 以及下面一些特殊的 Props。

＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 func_name 所傳回的值作為 Properties 所需的值。
範例如下:
set("light","@@test_light");
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;
}

"age"
------
設定值。
設定 player 在 mud 的活動時間。

"ghost"
-------
設定值。
當 值為 0， user 是 alive 狀態，反之，則 user is dead 
, 而且變成鬼。

"guild"
-------
設定字串。
設定公會物件。

"class"
--------
設定字串。
設定 user 所屬公會。

"wizard"
--------
設定值。
值為 1，此 user 為 wizard，反之，則否。

"ip"
-----
設定字串。
記錄 user 上次 login 的 ip address。

"cwd"
------
設定字串。
記錄 user 現今工作目錄。

"PATH"
-------
設定字串陣列。
設定 user 的 cmd path。


"reply"
-------
設定字串。
記錄 user reply 的對象。

"inactive"
----------
設定值。
設定是否為 inactive 狀態。
值為1,  則user 處於不可活動狀態。

"blind"
-------
設定值o
設定 user 是否為 blind 狀態。
值為 1，則為 blind。

"last_location"
---------------
設定字串。
設定 user 上次所在的位址。

"brief"
-------
設定值。
值為 1，則顯示簡短模式，反之，則否。


"spouse"
--------
設定字串。
設定 user's 伴侶。

"announce"
----------
設定字串。
設定字串為 "ON" or "OFF"。
設定 ON時, 可以看到 user login 、 user net-dead 和 user logout。

"wimpy"
-------
設定值。
設定當 hp 降到所設定的值以下，在作戰中會自動逃跑。

"wimpydir"
----------
設定字串。
設定 wimpy 時，逃跑的方向。

"quest"
-------
設定一 Mapping of (quests and their value)。
值為 1，表示此 user 已經完成該 quest。
例: set("quest/smith", 1);，表示已完成 smith's quest。

"snoopable"
-----------
設定值。
設定為 1 時，此 user 為可 snoopable，反之，則只有 admins 可 snoop
該 user。

"shell"
--------
設定字串。
 shell is a string which holds the user's interactive shell.  If none
is presently in use, it is set as "none".
 

"full_vision"
--------------
設定值。
設定亮度的等級， 用來決定 user 在黑暗中是否都能看的到。


"infravision"
-------------
 infravision is an integer used to determine if the user "sees"
with infravision when the room is dark.
 

"loudness"
-----------
設定值。
 loudness is an integer used by the say command to determine how
comparibly loud you sound to others in the room (See the say command).
 

"special_say"
-------------
設定字串。
將 say 改變成你想替換的字串。
例: set("special_say", "growl");，那麼當你 say hello，將會顯示
"***** growls: hello" 。

"no_follow"
-----------
設定值。
當值為 1，可防止 user/object 利用 user's 追蹤系統追蹤。 

"force_to_look"
--------------
設定值。
 Force_to_look is an integer which is 1 if the user was
last forced to look by an lfun, and 0 otherwise. If force_to_look is
1, then the look command checks the brief property (ie, when the
user moves) otherwise it does not (ie, when he types "look").
 

"home_dir"
----------
設定字串。
設定工作目錄。


                                 By Kitten...1994/9/24
