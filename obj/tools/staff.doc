房間編輯器的用法:
-----------------
1. 首先利用 mkroom 造出一個簡單的房間檔，goto 到那裡去

2. 使用 rset 設定房間的敘述，如 short, c_short, long, c_long 等。
   如果敘述很長的話，可以用 to 指令輸入。rset 可以用來設定大部分的房
   間變數，以下是幾個例子:

   > rset short "A hallway"
   Ok.
   > rset light 1
   Ok.
   > rset outside "farwind"
   Ok.
   > to rset long 
   Input reset of command: rset long ...
   To end message type ".". To abort, type ~q. To view help, type ~h.
   __________________________________________________________________
   "You are walking in a hallway made of pure white marble.
   As you stand, you can barely hear a low sound coming from the
   north wall.
   "
   .
   Ok.
   >

   你也可以用 connect 指令去接通房間的出口，語法是 connect 方向 to
   檔名。

3. 用 saveroom 將完成的房間儲存成 LPC 程式碼。這一段是房間編輯器嘗試
   將你目前所在的房間「還原」成 LPC 程式碼，所以最好儲存完畢之後自己
   檢查一下。最簡單的方法是打 update here 讓 driver 再編譯一次看有沒
   有錯。

   目前房間編輯器會嘗試去還原的資料有:
   
   short          (英文房間標題)
   long           (英文房間敘述)
   c_short        (中文房間標題)
   c_long         (中文房間敘述)
   light          (房間亮度)
   outside        (野外區域)
   exits          (房間出口)
   item_descs     (英文物品敘述)
   c_item_descs   (中文物品敘述)
   objects        (房間中的物件)
   
   如果以後有需要，可能會再加入其他的資料。
   
   若是你要存的檔案已經存在了，房間編輯器會問你是否要取代原來的檔案，
   請注意你原來的房間如果有除了上面所列的資料以外的 property，如 item_func,
   門、pre_exit_func、add_action 等都會消失不見，在儲存之前一定要確定這個
   房間只是普通房間，否則你原來所加的東西不見了，就頭大了。
   
4. 最後，強調一點:
   房間編輯器只是一個工具，你不可能只用房間編輯器就造出夠水準的區域，
   我做這個工具的目的只是提供一個輔助性的「房間修改工具」，真正最好用
   的編輯器還是文字編輯器。

   整個房間編輯器的程式碼都在 staff.c 裡，有耐心的人可以看一看，大部分
   是一些字串的處理與邏輯判斷，如果你想自己修改房間編輯器，可以作為參考
   。

                               - Annihilator -
