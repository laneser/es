
    頻道( channels )系統為巫師們的互相交流提供了簡便的管道. Channels 之名,
就像收音機擁有很多頻道一般, 因而得名. 我們可以收聽的頻道有很多,每一位巫師
可以隨時打開(tune in) 或關閉(tune out)某特定頻道,你可以同時收聽多個你喜歡的
頻道, 或完全不聽. 你也可以在你接收的頻道上傳播訊息, 而當時在收聽該頻道的巫師
都能收到你的訊息.
   
   現有的頻道如下:
wiz.  基本的巫師頻道, 以巫師的話題為主.
question.  專供發問及獲得解答的頻道.
gossip.  和 MUD 無關的東西.
mudlib.  專屬 mudlib 範圍的巫師用頻道.
driver.  專屬 driver 範圍的巫師用頻道.
doc.  專屬 documentation 範圍的烏施用頻道.
admin.  專屬 administer (系統管理者)的頻道, 非系統管理者不得打開此頻道.
gwiz.  這是在 UDP 網路上的 MUD 收聽用的.
interwiz. 這是在 TCP 網路上的 MUD 收聽用的.

   最下面的兩個頻道是透過網路來傳播訊息的, 除非你確定大家都會對你的訊息
感興趣, 不然最好不要用, 因為會佔據 CPU 而造成 lag!

   有關頻道的指令如下所示:
打開或關閉頻道, 使用 tune, 我們可以
    tune in
    tune out
    tune wiz
  
   最後一個指令將會相對的切換你的設定, 你可以打 "wiz <message>"來傳播你的
訊息; 如果你進入了其他的頻道, 也可以利用
    tune in
    tune out
來打開所有的頻道, 或把他們全關閉.
 
    你可以用 show 這個指令來顯示你在收聽中的頻道:
    show channels
   
    想知道某特定頻道上有哪些人正在收聽:
    show wiz
或是其他頻道的名稱. 總之, channels 這個指令能為你列出所有可能的頻道.
