                         基本物件 (Standard Objects)
                        =============================
                           檔案: /std/object/ob.c

I. 繼承物件 (Inherited Objects)

  None.

II. 性質 (Properties)

  "id" - Array of String, 物件的英文識別字。

  "name" - String, 物件的英文名字, 一律透過 set_name() 設定。

  "c_name" - String, 物件的中文名字, 一律透過 set_name() 設定。

  "short" - String, 物件的英文簡稱, 一律透過 set_short() 設定。

  "c_short" - String, 物件的中文簡稱, 一律透過 set_short() 設定。

  "long" - String, 物件的英文敘述, 一律透過 set_long() 設定。

  "c_long" - String, 物件的中文敘述, 一律透過 set_long() 設定。
  
  "weight" - Int, 物件的重量, 單位是「百公克」, 即 weight = 1 的物件重
             一百公克。

  "light" - Int, 物件是否會發光, 0 表示不會, 1 表示會。

  "max_load" - Int, 物件的最大載重量, 單位和物件的重量(weight)相同。

  "load" - Int, 物件的目前載重量, 單位和物件的重量(weight)相同。

III. 附加函數 (Applied Functions)

  int id( string str ) - 
    若 str 為此物件的識別字之一, 傳回 1, 否則傳回 0。

  int move( mixed dest ) -
    將此物件「移動」至 dest 的 inventory 中, dest 可為物件或物件的檔名
    字串, 移動之前會呼叫此物件目前所在環境的 release_object() 與 dest
    的 receive_object() 做檢查, 並且執行重量系統(weight, load, max_load
    )檢查。

  int remove() -
    將此物件從世界上「除掉」, 此物件所擁有的 inventory 會被移到此物件的
    環境中。

  int clean_up()
    當一個物件很久沒有被使用者使用到, driver 會呼叫此一函數試圖去將它釋
    放掉。

  int receive_object( object obj ) -
  int release_object( object obj ) -
  int check_light() -
  varargs void set_name( string name, c_name ) -
  varargs void set_short( string name, c_name ) -
  varargs void set_long( string name, c_name ) -
  void set_weight( int weight ) -
