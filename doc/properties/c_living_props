                    生物一般使用的 Props
                    --------------------

＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 funcname 所傳回的值作為 Properties 所需的值。
範例如下:
set("light","@@test_light");
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;
}

"hit_points"
------------
設定值。
設定 hp 的現值。0 以上為 alive，-1 為 dead。
但是不可以改變此值來處理傷害。傷害應以 receive damage() 處理。

"max_hp"
---------
設定值。
設定 hp 的上限。若不設定，則 max_hp = constitution * 10 + 20。


"spell_points"
--------------
設定值。
設定法力現值。

"max_sp"
---------
設定值。
設定 sp 的上限。若不設定，則 max_sp = int * 10 + 20。

"force_points"
--------------
設定值。
設定內力現值。

"max_fp"
---------
設定值。
設定 fp 的上限。若不設定，則 max_fp = skill(inner-force) * force_effect 。

"talk_points"
--------------
設定值。
設定談話點數現值。

"max_tp"
---------
設定值。
設定 tp 的上限。若不設定，則 max_tp = 30+level * 30 

"npc"
-----
設定值。
值為 0，為 user;值為 1，為 npc。
不能改變。 

"weapon1" & "weapon2"
---------------------
設定物品。
設定右、左兩手所持武器。此 prop 不應直接設定，
應透過 wield_weapon() 來設定。
例:wield_weapon( "/d/noden/bluesand/item/creset_cutlass");
   wield_weapon2( "/d/noden/bluesand/item/hook");
   則 當你 data 此 mob
 
   weapon1         : OBJ(creset cutlass
   /d/noden/bluesand/item/creset_cutlass#48453)
   weapon2         : OBJ(steel hook /d/noden/bluesand/item/hook#48454)


"natural_armor_class"
---------------------
設定值。
設定自然的防護能力(AC)。此值並非為總 AC 。此值將利用
calc_armor_class() 加入總 AC 。欲獲得更進一步資料，
請看 docs about combat。


"natural_defense_bonus"
-----------------------
設定值。
設定自然的防護值(DB)。此值並非為總 DB 。此值將利用
calc_armor_class() 加入總 DB 。欲獲得更進一步資料，
請看 docs about combat。

"natural_weapon_class1"
-----------------------
設定值。
此值為右手自然的 weapon_class(WC)。此值並非為總 WC 。此值將利用
calc_weapon_class() 加入總 WC。 假如一回合能攻擊兩次，那麼你也可
以設定natural_weapon_class2，給予左手 WC。欲獲得更進一步資料，
請看 docs about combat。


"natural_min_damage1" & "natural_max_damage1"
---------------------------------------------
設定值。
設定右手自然傷害的下、上限。若一回合能攻擊兩次，那麼你也可以設定
natural_min_damage2 & natural_max_damage2，給予左手自然傷害的下、上限。
這些值都將利用 natural_weapon_class() 加入 min_damage & max_damage。

"armor"
-------
設定 Mapping of (types of armor and armor's files)。
此 prop 不應直接設定，應透過 equip_armor() 來設定。
例: equip_armor( "/d/noden/moyada/obj/snake_plate");
    equip_armor( "/d/noden/moyada/obj/dragon_shield");
    則 當你 data 此 mob

    armor         : ([ "body" : OBJ(snake platemail
    /d/noden/moyada/obj/snake_plate#48247), "shield" :
    OBJ(dragon shield /d/noden/moyada/obj/dragon_shield#48248)
    ])


"no_attack"
-----------
設定值。
當此值為 1 時，此 mob 為不可殺狀態。

"gender"
---------
設定字串o
設定性別o

"race"
------
設定字串o
設定種族o 種族的設定會影響 mob's language。
在設定 mob 種族前請 help race_type, 查詢已有定義的種族,
如果你要設的種族尚未被定義, 請mail admin to set it 。
 
"stop_attack"
-------------
設定值。
防止 mob 在戰鬥中做正常攻擊。比方講，當你在施展魔法時，中間會有
幾個回合不能做正常攻擊。而此值就是設定有幾個 heartbeat， mob 
不能做正常攻擊。每經過一個 heartbeat，此值將會減一，當此值歸零時
則恢復正常攻擊。此值不可直接設定，而是利用 block_attack() lfun
設定。 

"wealth"
---------
設定一 Mapping of types of (coins and values)。
設定此生物所擁有的貨幣種類和數目。
詳細資料請看 /doc/mudlib/money。
例: set( "wealth/silver", 100);
    set( "wealth/copper", 200);
    則此生物擁有 100 個銀幣和 200 個銅幣。

"dark_sight"
------------
設定值。
設定 living 在黑暗中是否可以看得見,在無光源情況下。


                                   By Kitten...1994/9/24
