#include <mudlib.h>

inherit WEAPON;

void create()
{
    set_name("Thounder star","地母雷公轟");
    add("id", ({ "star","thounder star" }) );
    set_short("地母雷公轟");
    set_long(@C_LONG
一柄八角小□，□柄長僅及尺，□頭還沒常人的拳頭大，一般人看來小巧玲瓏倒像
是孩童的玩具一般，用以臨敵，看來全無用處。只有行家才看的出，這種兵器是青
城派雙絕之一的雷公轟，而你手上拿的這柄雷公轟異常沈重，且□頭銀光閃閃，熠
□掩日，必是神兵。
C_LONG
    );
    set( "unit", "把" );
    set( "weapon_class", 30 );
    set( "type","chain");
    set( "min_damage",15);
    set( "max_damage",30);
    set( "weight",270);
    set( "wield_func","weapon_wield");
    set( "unwield_func","weapon_unwield");
    set( "value", ({ 2300, "gold" }) );
}

void weapon_wield()
{
  object holder;
  int my_ali,holder_level;

  holder=environment(this_object());

  if (my_ali=(int)holder->query("war_score")>20000) {
    holder_level=(int)holder->query_level();
    set("min_damage", 8+holder_level);
    set("max_damage", 26+holder_level);
    set("weapon_class", (holder_level*5/2));
    set("hit_func","special_attect");
    }

}

void weapon_unwield()
{
    set("min_damage", 15);
    set("max_damage", 30);
    set("weapon_class", 30);
    delete("hit_func");
}

int special_attect(object victim,int damage)
{
   object holder;
   int my_str,vic_kar,dam,my_score,degree;

   if( !victim ) return 0;

   if( !(holder = environment(this_object())) || !living(holder) ) return 0;
   
   if( holder->query("spell_points")< 4) return 0;
   my_str=holder->query_stat("str");
   vic_kar=victim->query_stat("kar");
   vic_kar=vic_kar<5?5:vic_kar;
   degree=3*my_str-2*vic_kar+10;
   if( random(degree)<vic_kar ) return 0;

   my_score=holder->query("war_score");
   dam= ( my_score-10000)/1000;
   if ( dam>40 ) dam=40;
   victim->receive_special_damage( "electric",dam );
   victim->set("last_attacker", holder );
   holder->add("spell_points",-4);
   tell_object( holder,
         sprintf("\n你的雷公轟開始隆隆作響....\n       只聽霹靂一聲，%s\n",
         set_color("爆出一團金光炸的敵人血肉橫飛。","HIY"))
         );
   tell_room( environment(holder),
    sprintf("\n%s的雷公轟開始隆隆作響....\n      只聽霹靂一聲，%s\n",
         holder->query("c_name"),
         set_color("爆出一團金光炸的敵人血肉橫飛。","HIY")),holder );
   return 5;
}
