
寵物(/std/pet.c)
----
寵物是NPC 的一種, 基本上, 它是你生活上的夥伴, 你可以要它幫你帶一些物品,
可以要它跟隨你或不跟隨你, 你也可以馴服一些野生的 npc 來成為你的寵物,
有些寵物甚至可以當作坐騎, 可以協助你作戰。
繼承物件: /std/monster.c
一、寵物的額外Properties( except monster's Properties) 說明:

    1.pet_type        : 設定一字串。
                        設定寵物的 type。 一個 player同時只能擁有一隻同種
                        type的寵物。
    2.following       : 設定一定值。
                        假如此 pet沒有主人時,此 property 沒有作用。
                        否則, 當值為 1時, 將會跟隨主人移動, 值為 0時則否。
    3.mountable       : 設定一定值。
                        值為1 ,此 pet可以被騎; 值為0 ,則否。
    4.wild            : 設定一定值。
                        值為1 ,此 npc可以被當成寵物;
                        值為0 ,則為野性仍在的npc ,不可當成寵物。
    5.mounted_load    : 設定一定值。
                        所設定的值為當此pet 被騎時, 它所能負載的總重量。
                        當user 騎上此pet 時, 其總負載將等於此值減掉 user
                        本身的重量。
    6.master_necessary: 設定一定值。
                        當值為1 時, 當此 npc或pet 被馴服或騎上時將會認定
                        該 player為其主人; 當值為0 ,則該寵物將不會有主人。
    7.tactic_func     : 設定一func_name。
                        default func_name 為 call_help。
                        該func 的作用在於, 當pet 遭受攻擊時, 假如 master
                        在同一房間, 沒有進入戰鬥狀態, 而且 level>5時, 則
                        master 將會加入作戰幫助 pet。

二、寵物所賦予的額外指令:

    1.lead     : 引導以你為master 的寵物跟隨你走。

    2.release  : 命令以你為mater 的寵物不要跟你走, 留在原地。

    3.put      : 將物品放置於已被馴服的寵物身上。

    4.get      : 將物品從以你為master 的寵物身上拿回來。

    5.unwild   : 嘗試馴服尚未被馴服的 pet。

    6.mount    : 嘗試騎上已被馴服的 pet。

三、寵物的基本 function:

    1.lost_master  : 將pet remove 掉。

    2.relay_message: 當接受到msg 時, 所產生的反應。除了 monster.c 的設定外,
                     pet.c 在有master 的情況下增加了幾種反應。
                     (1). 主人不見時, 將會執行 lost_master。
                     (2). 主人要求 pet 跟隨時,當收到主人移動的訊息時,
                          會自動跟上 。
                     (3). 當主人 quit 時, pet 將會被remove。

    3.die          : 當pet 死的時候, 將會通知主人, 並將主人身上有關寵物的
                     資料移去。

    4.set_master   : 設定pet 的master, 將主人的名字加在pet 名稱前, 並將
                     pet 的資料加在master 身上。 

    5.set_mounted  : 設定 player 騎上 pet 之後, 會做的一些基本動作。


以上所有的 function 和指令都可以同名稱的function 和指令覆蓋過去, 若要保留
原有功能只需在同名 func 中加上    pet::func_name(mixed *arg) 即可。


                                   By Kitten 1994/9/24
