                         防具所使用的 Props 
                        --------------------
防具使用標準 object 所包含的所有 Props 以及下面一些特殊的 Props。

＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 funcname 所傳回的值作為 Properties 所需的值。
範例如下:
set("light","@@test_light");
              
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;
}

"type"
------
設定字串。
type 控制了防具所穿的位置。每一種 type 只能穿一件。
type 種類與詳細資料請利用 "help armors_standard" cmd 查詢。

"armor_class"
-------------
設定值。
armor_class 是 armor 的計量單位。 數值的大小將會影響穿透力。

"defense_bonus"
---------------
設定值。
defense_bonus 是 armor 的計量單位。 數值的大小將會影響可減少的傷害。

"special_defense"
-----------------
設定一 mapping of (type  of magic and number)。
設定armor 的魔法防禦, 數值的大小將影響所減少的傷害比例。
詳情請以 "help armors_standard" 查詢。

例: set( "special_defense", 
            ([ "fire":10, "cold":-15, "energy":-5"]) );
           
"extra_skills"
-------------
設定一 mapping of (type  of skill and number)。
設定防具是否具有技能的加強能力。

例: set( "extra_skills", 
			([ "parry":10, "block":-5 ]) );
     
"extra_stats"
-------------
設定一 mapping of (type  of stat and number)。
設定防具是否具有屬性的加強能力。

例: set( "extra_stats",
            ([ "str":1, "dex":-1 ]) );

以上所有設定值請參照標準 help armors_standard。
                  
"equipped"
----------
設定值。
當設定值為 1 時，表示此防具已被穿上，反之，則否。

"equip_func"
------------
設定func_name。
設定當此 armor 被穿上時所要額外執行的函數名稱。
你必須在同一檔案或其inherit 的檔案內設計此函數。

"unequip_func"
--------------
設定func_name。
設定當此 armor 被卸下時所要額外執行的函數名稱。
你必須在同一檔案或其inherit 的檔案內設計此函數。

"wear_msg"
----------
設定字串->不論中英, 或是字串陣列 -> ({ "英文", "中文" })
表是穿上時所出現的訊息, 未定則用內定的動詞組合出穿戴訊息。

"material"
----------
設定字串。
設定防具質料，質料的種類請用 help armor_materials。

例: set( "matrial", "light_metal" );

Functions:
stop_equip(object owner): 可用來決定 owner 是否能穿此裝備,
return 0->yes, return 1->no.

                                   By  Kitten....1994/9/24
