每個玩家都有一個「探險度」的 mapping, 定義於 /std/user/explore.c, 以下
的 function 存在於該檔下:
 
int set_explore(string explore);
 
    這個 function 會設定玩家的探險度. 探險度 daemon 會檢查這個探險點
    是否合法. 如果玩家是第一次得到這個探險點, 並且是一個合法的點數,
    則此 function 會傳回 1, 否則傳回 0.
 
int query_explore(string explore);
 
    這個 function 傳回 1 如果玩家已經有設有這個探險點並且是個合法的點
    數, 否則傳回 0.
 
這個系統使用 "explored" 指令來告知玩家的探險度資料. 詳情請參考
"explored" 指令的 help. 這個指令也能讓 admin 將一個探險點合法化. 這一個
步驟應於該區域通過 QC 後執行.
 
