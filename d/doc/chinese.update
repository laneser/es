rooms:

set_name("English", "中文");
	(這裡的 English 是物品名稱後面括號裡的名字.)
set_short("中文");
set_long("中文敘述\n");
set("item_desc", ([
	"某樣東西" : "某樣東西的敘述.\n",
	]) );

object:
set_name(), set_short() & set_long() same as rooms.
set("extra_look","中文敘述");

boards:
set("long.text","這是一個留言版..\n");
set("short.text","留言版");

doors:
on create_door() --
用 "desc" : "這是一個門" ... 舊有 code 裡的 c_desc 還是可以用, 但請
從現在開始用 desc.
注: 你還是要 name, c_name.. name 是用來顯示 () 裡的 keyword.

virtual rooms:
參考 /d/noden/noden.map 的 format. 將 short, long 填入中文.


command changed/updated:
inventory
look

drop
get
wield
unwield
wear
remove
give
