    

    這個Mud 系統的函式庫提供了一個可編輯的表情(soul)指令, 可幫助你隨意創造新
的表情型態而不需去改變系統的檔案. 以下有幾個指令可幫助你瞭解什麼表情指令是可
以使用的, 及使用後你會看到的動作.

    第一個較方便的指令是"semote". 這個指令能列出資料庫中現有的表情指令, 當下
達一個要求(如"semote shrug")時, 系統會顯示目前這個指令的資料庫條目( 大部分列
在下面的條目 ).
 
    動作的模式分為 目標(targeted)及 無目標(untargeted)兩種. 目標模式的指令需
直接用到其他玩家身上: 如"worship <name>"; 無目標模式的指令則不須用到其他玩家
身上, 如"think". 大多數的表情指令都包含上述兩種模式, 如"nod"這個指令一樣; 有
些只有無目標模式, 如"think"; 而有些則只有目標模式, 如"leer".無目標模式的動作
參考方式是直接打表情名稱(name), 目標模式的參考方式則是在動作名後加/t (name/t)  
. 因此, 當你打"semote shake"時, 你就能看到"shake"指令的無目標模式; 打"semote 
shake/t"時就能看到目標模式. 如此就能因目標的存在與否,而使得同一個指令能擁有不
同的輸出. 再拿"shake"當例子, 無目標模式的輸出是" 你搖搖你的頭 ", 而目標模式則
是" 你抓住<c_name>的身體一陣亂搖 ".

    接下來是"temote", 當你使用這個指令時, 將能看到你, 目標, 及房間中第三者會
見到的指令輸出模式. 舉例來說, 下達"temote shake emphatically"的指令時, 所顯示
的輸出分別是:
 me:
 你搖搖你的頭.
 others:
 Tsunami shakes her head emphatically.
 c_others:
 津名魅搖搖她的頭.
    
    這項功能幫助我們在創造新的表情動作指令時, 不需要詢問別人關於指令的輸出,
就能輕易的偵錯.

    關於 "edemote", 其功能是用來創造新表情動作指令的資料庫. 輸入 "edemote
thwack" 來幫助你創造一個叫 "thrack" 的新指令供玩家使用; 或輸入 "edemote
thrack/t" 將其設定成目標模式的表情動作指令. 舉個例子, "kiss/t" 指令的條目是:
 
 .me
 $N $V $n $M
 .other
 $N tilts $P head as $S kiss $n $M
 .target
 $N $V you $M
 .modifier
 chastely
 .end
    
    其中 .me 代表自己會看到的格式, .other 代表別人會看到的格式, .target 代表
你指令所用者會看到的格式, .modifier 則是為使用者預留一個修飾用空缺,好讓使用者
可以自由決定後再鍵入. 如果使用者打 "kiss psyche", 結果會是:
 You kiss Psyche chastely.
但如果使用者打 "kiss psyche passionately", 結果會變成:
 You kiss Psyche passionately.

    如果 .others 和 .target 都沒有設定, 那系統就會自動使用 .me, 我們可以注意到
.me 中有 $N 和 $V 的設定.
    
    $[A-Z,a-z]是為了在表情指令中填入適當的名字(name)/代名詞(pronouns)/
介係詞(preposition) 而特別設定的空缺. 在上面的例子中, $N是用來代表使用者的名字
, $V 是用來代表動詞(kiss)的, $n 則是代表目標物的名字, 而 $M 則是可任意選擇的修
飾詞.
    
    以下所列的是一些可能會用來代換的參數格式:
  $N = 使用者的名字, $n = 目標物的名字,
  $S = 主格(使用者), $O = 受格(使用者), $P = 所有格(使用者)
  $s = 主格(目標物), $o = 受格(目標物), $p = 所有格(目標物)

  $M = 副詞(只有當使用者以" ."終止命令時才會出現)
  $m = 副詞(如果使用者不以" ."結尾, 則會一直存在)
  $V = 動詞(相當於"表情動作"的名稱, 也可被設成 .verb)
  $G = 使用者的所有格 (相當於 $N's 或 "your")
  $g = 目標物的所有格 ('' $n's '')
  $Q = 使用者的前述所有格 (如 hers, his, its, hirs, yours)
  $q = 目標物的前述所有格 ( '')
 
  $S, $O, $P, $s, $o, $p等的用法:
  主格 = (he, she, it, sie), 
  受格 = (him, her, it, hir),
  所有格 = (his, her, its, hir)
   
       支援的檔頭都列在 /include/emoted.h中.

    