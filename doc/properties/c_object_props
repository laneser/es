                        物品所使用的 Props
                        ------------------

＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 funcname 所傳回的值作為 Properties 所需的值。
範例如下:
set("light","@@test_light");
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;
}

"name"
-------
設定字串。
設定物品的英文名稱。

"c_name"
--------
設定字串。
設定物品的中文名稱。

＊可利用 set_name( "English_name", "Chinese_name"); 來取代 
  set( "name", "English_name") ;
  set( "c_name", "Chinese_name) ;

"cap_name"
----------
設定字串。
設定物品的大寫英文名稱。

"c_cap_name"
------------
設定字串。
設定物品的大寫中文名稱。

＊可利用 set_name( "English_name", "Chinese_name"); 來取代
  set( "name", "English_name") ;
  set( "c_name", "Chinese_name) ;
  set( "cap_name", "English_name") ;
  set( "c_cap_name", "Chinese_name") ;

"plural"
--------
設定字串。
一個物品可以有多個名字，而 set("plural", "another_name") 可以
設定物品額外的名字。

"short"
--------
設定字串。
設定物品的英文 short_description，也就當你看到這個物品時它所顯示的描述。

"c_short"
---------
設定字串。
設定中文的 short_description。

"long"
-------
設定字串。
設定物品的英文 long_description，也就當你 look 這個物品時它所顯示的描述。


"c_long"
--------
設定字串。
設定中文的 long_description。


"id"
----
設定字串陣列。
設定當我們使用 present() function 時所欲傳回來的字串陣列。
當我們使用 set_name 時，其實已經將 name 設定為 id 之一了。
因此在 set id 中只需設定除 name 外的 id 即可。
例: set( "id", ({"biff", "bam", "kappow"}) );

"light"
-------
設定值。
設定物件是否為光源。
例:set( "light", 1);， 則物件為光源，反之，則否。

"unit"
------
設定字串。
設定該物件的計數單位。

"weight"
--------
設定值。
設定物件重量，以 0.1 kg 為單位。
例:set( "weight", 100);，則物件為 10 kg 重。

"max_load"
----------
設定值。
設定最大的裝載重量，以 0.1 kg 為單位。只有當物件 inherit CONTAINER 時
才需設定此值。

"extra_look"
------------
設定字串。
當你 look 帶有設定此物件的 player 時，你將會多看到所設定的英文字串。

"c_extra_look'
--------------
設定字串。
你 look 帶有設定此物件的 player 時，你將會多看到所設定的英文字串。

"invisible"
-----------
設定值。
設定一個值以便決定物件的 invisible 狀態。值為 0，所有人都可看到此物件，
值為 1，players 不可看到此物件，值為 2，只有 admins 可看到此物件。
假如你無法看到該物件，你也無法看到該物件所傳的訊息。因此若設定某 
aggressive mob 是 invisible to players 的，則 players 將無法知道該 mob
正在打你。 

"prevent_drop"
---------------
設定值。
值為 1 ，設定物件為不可 drop 。 

"prevent_get"
-------------
設定值。
值為 1， 設定物件為不可 get 。

"prevent_insert"
-----------------
設定值。
值為 1， 設定物件為不可被放入容器中。

"silent_look"
-------------
設定值。
當設定值為 1， 當有人 look 此物件時，其他人將不會收到他在看該物件的訊息。


                                              By Kitten...1994/9/24
