                         武器的 Props
                         ----------------
武器除了使用標準 object 設定的所有 Props 外，還可使用下列的 Props 。

＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 funcname 所傳回的值作為 Properties 所需的值。
範例如下:
set("light",@@test_light);
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;
}

"weapon_class"
--------------
設定值。
值越大，命中的機率越高。

"min_damage"  &  "max_damage"
-----------------------------
設定值。
設定武器所造成傷害的上、下限。
造成的傷害是 Random(max_damage-min_damage)+min_damage。


"type"
------
設定字串。
設定武器類別， 各種武器類別必須配合該武器技能方可裝備(if weapon_class >15)。
In the default mudlib, 在攻擊時武器使用必須使用該武器技能。假如， 修改了
skill system，也必須對此做修正。 你可以用 "help weapons_standard"
得知目前已被定義的武器類別。

"second"
--------
設定值。
值為 1 ，表示可當作副武器使用。反之，則否。

"nosecond"
----------
設定值。
值為 1 ，表示必須雙手使用。反之，則否。

"wield_func"
------------
設定func_name 。
設定當裝上武器時會額外執行的 func。
此func 必須於同檔案或其所 inherit 的檔案中被設定。


"unwield_func"
--------------
設定func_name。
設定當卸下武器時所會額外執行的 func。
此func 必須於同檔案或其所 inherit 的檔案中被設定。

"wielded"
---------
設定值。
值為 1， 表示武器已被裝上。反之，則否。

"bleeding"
---------
設定值。
值表示武器會造成流血效果的機率。最好不要超過 25。

"hit_func"
----------
設定 func_name。
設定武器的特殊攻擊, 假如該 func 傳回的值為 1 時
, 執行該func, 否則為一般攻擊。
此func 必須於同檔案或其所 inherit 的檔案中被設定。

Functions:
stop_wield(object owner): 可用來決定 owner 是否能裝備此武器,
return 0->yes, return 1->no.

                                         By Kitten 1994/9/24
