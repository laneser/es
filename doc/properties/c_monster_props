                     Monster 一般所使用的 Props 
                     --------------------------

Monster 使用標準 living 所包含的所有 Props 以及下面一些特殊的 Props。

＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 funcname 所傳回的值作為 Properties 所需的值。
範例如下:
set("light","@@test_light");
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;
}

"unit"
------
設定字串。
設定計量的單位。

"aggressive"
------------
設定值。
設定是否為主動攻擊(但是必須是 user由外進入)。
值為 1，則可能主動攻擊, 機率由 user's 運氣控制。

"killer"
--------
設定值。
設定是否為主動攻擊(只要 user 在同一房間)。
值為 1，則為主動攻擊。

"c_killer_msg"
--------------
設定字串。
設定主動攻擊的中文訊息( killer or aggressive) 。
若不設定, 則採取 default 訊息。
例 :
   set( "c_kill_msg", "突然間,小貓用它銳利的爪子向你抓了過來!!!" );
   則當 此 mob 向你主動攻擊時，你將會看到下面的訊息：
   
   突然間,小貓用它銳利的爪子向你抓了過來!!! 

"forgetful"
-----------
設定值。
當值為 1，mobs 將不會 hunt player。反之，則否。

"pursuing"
----------
設定值。
值為 1 時，當 user 在與此 npc 作戰中途逃跑，則此 npc 會追過去。
由於它是利用 relay_message() 抓 user 離開的方向，所以， 只要
player 連續走兩次， npc 就會追丟。

"moving"
---------
設定值。
設定 mob 是否可以移動。值為 1，	則 mob 以一定的速度移動。

"speed"
--------
設定值。
設定每幾個 heart_beat 移動ㄧ次, 勿設在 20 以下
, 以避免系統負擔過重。


"patrol"
---------
設定字串陣列。
設定一個字串陣列以提供 mob 移動的方向。
例: set( "patrol", ({ "north", "south"}));，則 mob 會照著
    north,south,north,south,north,south....順序移動。

"chat_chance"
-------------
設定值。
設定 call chat_output 的機率。

"chat_output"
-------------
設定字串陣列。
設定 mob 在平時說話的字串陣列。

"att_chat_output"
-----------------
設定字串陣列。
設定 mob 在攻擊時說話的字串陣列。


"exp_reward"
------------
設定值o
設定打死怪物所獲得的經驗。

"tactic_func"
-------------
設定 func_name。
每一次 heart beat 都會去 call 一次 tactic_func，如果傳回來的值是 0
，則會 run 一般攻擊，反之，則此回合，將不會採正常攻擊。
此func 必須於同檔案或其所 inherit 的檔案中被設定。


                                 By Kitten...1994/9/24
