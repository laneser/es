                      建立房間所提供的 Properties
                  ------------------------------------
＊以下所有 Properties 所需設定的值都可以 "@@funcname" 來取代。
  系統將以 funcname 所傳回的值作為 Properties 所需的值。
範例如下:
set("light","@@test_light");
int test_light()
{
  if(  (string)this_player()->query("gender") == "female" )
          return 1;
  return 0;	     
}  

依此設定則只有 female 能看到此房間。

  
"exits"
-------
設定一 Mapping of (directions and room's filenames)。
設定房間的出口。
範例如下:
set( "exits", ([ "north" : "/d/TMI/partyroom.c" ]) );
設定北邊出口為 /d/TMI/partyroom.c

add( "exits", ([ "south" : "/d/TMI/drunktank.c" ]) );
增加南邊的出口，通往 /d/TMI/drunktank.c

delete( "exits/south" );
刪除南邊的出口


"no_vision" , "no_vision_msg", "no_vision_c_msg"
-----------
使用 no_vision 可攔截此房間的 long_description,而以 
no_vision_c_msg 以及 no_vision_msg 取代房間的中、英
敘述。
範例如下:
set("no_vision",1);
set("no_vision_c_msg","你身處一片雲海之中，無法看見任何東西。");
set("no_vision_msg", "You can't see anything here.");

"item_desc"
-----------
設定一 Mapping of (name and descriptions of things)。
利用此 Prop 我們可以設定一些房間的物品以及物品描述而不需要 clone
出真正的物品。
範例如下:
set_long( "This is a room with a single chair in the middle" );
set ("item_desc", ([ "chair" :
"This chair is very rickety and would collapse if you sat in it." ]) ) ;
那麼，如果有 players 走進此房間並 look chair，他們將會看到 
The chair is very rickety and would collapse if you sat in it .
而不需要有 chair 的實體。

"c_item_desc"
-------------
與 item_desc 用法相同，但是只適用 chinese mode。

"item_func"
-----------
設定一 Mapping of (names of things and func_name when you look )。
此 Prop 可設定當 player 看物品時，你想要讓他發生什麼事。
範例如下:
set ("item_func", ([ "painting", "teleport" ]) ) ;
int teleport() {
	this_player()->move_player("/d/noden/farwind/bank.c", "SNEAK");
    return 1;
}
如此設定的話，當 player lookt at painting 時，此 player 將會被傳送到
bank。假如使用 item_func, 則可將欲寫在 item_desc & c_item_desc 的訊息
，寫在 item_func 中，切忌同時使用 item_func 和 item_desc。
func 必須於同檔案或其所 inherit 的檔案中被設定。
  

"objects"
---------
設定一 Mapping of ( ids and filenames of objects)。
設定在 reset 時，此房間將會產生物品或mobs。
範例如下:
set ("objects", ([ "orc" : "/obj/orc", "chair#1" : "/obj/chair",
"chair#2" : "/obj/chair" ]) ) ;
則在 reset 時，此房間將會補充 orc 和兩張椅子中不足者。

"pre_exit_func"
---------------
設定一 Mapping of (directions of leave and func_name before leaveing)。		
利用此 porp，可以檢查 player 是否可以從設定的方向離開。
範例如下:
set ("pre_exit_func", ([ "north" : "foo" ]) ) ;
在 player 往 north 方向離去前，會先檢查 foo function 
傳回來的值。若值為 1 則不可以由 north 離開。
func 必須於同檔案或其所 inherit 的檔案中被設定。

"post_exit_func"
----------------
設定一 Mapping of (directions of leave and func_name after leaveing)。
和 pre_exit_func 用法相同o 唯一不同的是，pre_exit_func 是移動前跑function
，而 post_exit_func 則是移動後執行function。
func 必須於同檔案或其所 inherit 的檔案中被設定。

"exit_c_msg", "exit_msg"
------------------------
設定一 Mapping of (directions and descriptions of leaveing)。
設定移動時的中、英訊息。
範例如下:
set ("exit_c_msg", (["north' : "$N悠閒地離開。\n"); 
set ("exit_msg", ([ "north" : "$N leaves slowly.\n") ;
當 player (kitten) 由此房間往北離開時，此房間的其他 players 將會看到
"做苦工中的 君 悠閒地離開。" 的中文訊息。
"Kitten leaves slowly."  的英文訊息。 

"x_coordinate" & "y_coordinate"
-------------------------------
設定值。
設定地圖上房間的 X、Y 座標。

"virtual_server"
----------------
設定值。
值為1, 設定此房間為 virtual_room。

"no_cast"
---------
設定值。
值為1, 設定此房間不可施魔法。

"no_enhant"
設定值。
值為1, 設定此房間不可施展武功。


                                      By Kitten...1994/9/24
