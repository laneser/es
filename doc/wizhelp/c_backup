人物資料備份 :
    一般來說，需要用到備份資料的時機只有一個，就是因為跳機所
產生的人物資料錯誤或消失，在這個時候，那些可憐的人的資料檔 (
只有資料檔)會消失，而使得這些人在 login 後會因為找不到資料檔
遂變成無名氏( 如果連線檔不在了，那他根本就得造新人物了，也就
不會有其他問題了 *grin*)，所以，我們針對這點，為 Player 增加
了自動備份的功能，時機是每次他復活之後，這時，會把他的最新狀
況 Copy 到 /data/backup/user_ob 底下，按照種族和名字排好，所
以，當這個人物需要取回備檔的時候，只要在他 QUIT 之後，把他的
備份 Copy 回 /data/std/user_ob 底下相對的位置就好，不過這個動
作，目前只開放給神來執行，以免混亂。

    當然，在 Player suicide 之後，他的備份檔也會被自動刪除。

    此外，巫師的資料備份請直接下 backup 命令，同樣的，資料之
取回仍由神管理。

					By Ruby , 1-27-95
